If the user asks about modifying the Pi harness, invokes a harness-related skill
(e.g. `/document-self-modif`), or works on extensions/patches/docs under
`~/.pi/agent/`, read `~/.pi/agent/CONTEXT.md` to access the harness configuration
(if not already loaded in this conversation).
